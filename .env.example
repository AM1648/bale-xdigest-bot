TZ=Asia/Tehran
BALE_TOKEN=123456789:your-bot-token
RAPIDAPI_KEY=your-rapidapi-key
# Optional SOCKS5 proxy for RapidAPI + tweet media only (Bale traffic is always direct).
# Leave empty when no proxy is needed. With the proxy on the Docker host:
# PROXY_URL=socks5://host.docker.internal:20170
PROXY_URL=
