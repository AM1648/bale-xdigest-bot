services:
  bot:
    build: .
    restart: no
    env_file: .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./.state:/app/state
    extra_hosts:
      - "host.docker.internal:host-gateway"
